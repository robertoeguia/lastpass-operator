---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: lastpasscustoms.edgelevel.com
spec:
  group: edgelevel.com
  names:
    kind: LastPassCustom
    listKind: LastPassCustomList
    plural: lastpasscustoms
    singular: lastpasscustom
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LastPassCustom is the Schema for the lastpasscustoms API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LastPassCustomSpec defines the desired state of LastPassCustom
            properties:
              customRef:
                properties:
                  group:
                    type: string
                  name:
                    type: string
                required:
                - name
                type: object
              syncPolicy:
                properties:
                  enabled:
                    type: boolean
                  refresh:
                    description: |-
                      A Duration represents the elapsed time between two instants
                      as an int64 nanosecond count. The representation limits the
                      largest representable duration to approximately 290 years.
                    format: int64
                    type: integer
                required:
                - enabled
                - refresh
                type: object
            required:
            - customRef
            type: object
          status:
            description: LastPassCustomStatus defines the observed state of LastPassCustom
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
